# Venice Auth Companion

Source for the optional, explicitly authorized Chrome companion. Setup creates the manifest and a private local pairing configuration; never commit generated files or captured authentication.

See [Classic transport and browser pairing](../../docs/providers/venice-browser-classic.md) for installation, permissions, memory-only secret handling, refresh behavior, current verification and limitations.
